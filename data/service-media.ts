import type { ServiceMedia } from "@/types";

/**
 * Job photographs filed under service folders. Empty until real service-folder
 * imports are generated — heroes fall back to photos.ts via stockHero.
 */
export const serviceMedia: ServiceMedia[] = [];
